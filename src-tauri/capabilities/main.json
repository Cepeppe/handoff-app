{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Least privilege for the overlay window: read the app version, exchange events with the Rust side, and read the window geometry. Every later task adds the single permission it needs and nothing more — window setters, clipboard, notification, opener, global shortcut and autostart are all absent on purpose.",
  "windows": ["main"],
  "permissions": ["core:app:default", "core:event:default", "core:window:default"]
}
